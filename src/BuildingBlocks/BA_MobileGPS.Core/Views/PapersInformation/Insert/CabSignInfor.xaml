<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="BA_MobileGPS.Core.Views.CabSignInfor"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:i18n="clr-namespace:BA_MobileGPS.Core.Helpers;assembly=BA_MobileGPS.Core"
    xmlns:input="clr-namespace:Syncfusion.XForms.TextInputLayout;assembly=Syncfusion.Core.XForms"
    xmlns:prism="http://prismlibrary.com"
    prism:ViewModelLocator.AutowireViewModel="True">
    <ContentView.Resources>
        <Style TargetType="input:SfTextInputLayout">
            <Setter Property="ContainerBackgroundColor" Value="{DynamicResource WhiteColor}" />
            <Setter Property="ContainerType" Value="Outlined" />
            <Setter Property="OutlineCornerRadius" Value="30" />
            <Setter Property="ErrorColor" Value="{DynamicResource DangerousColor}" />
        </Style>
    </ContentView.Resources>
    <ContentView.Content>
        <StackLayout Padding="15,5,15,20" Spacing="5">
            <Label
                HorizontalOptions="Start"
                IsVisible="{Binding CreateButtonVisible}"
                Text="{Binding AlertMessenger}"
                TextColor="{Binding AlertMessengerColor}" />
            <!--  Số phù hiệu  -->
            <Label HorizontalOptions="StartAndExpand" VerticalOptions="Start">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{i18n:TranslateResource CabSignInfor_Label_SignNumber}" TextColor="{DynamicResource TextPrimaryColor}" />
                        <Span Text=" (*)" TextColor="OrangeRed" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <input:SfTextInputLayout
                ErrorText="{Binding SignNumber.ErrorFirst}"
                HasError="{Binding SignNumber.IsNotValid}"
                IsEnabled="{Binding CanEditPaperNumber}"
                VerticalOptions="Start">
                <Entry
                    MaxLength="100"
                    Placeholder="{i18n:TranslateResource CabSignInfor_Label_SignNumber}"
                    Text="{Binding SignNumber.Value}" />
            </input:SfTextInputLayout>
            <!--  Ngày đăng kí  -->
            <Label HorizontalOptions="StartAndExpand" VerticalOptions="Start">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{i18n:TranslateResource CabSignInfor_Label_RegisterDate}" TextColor="{DynamicResource TextPrimaryColor}" />
                        <Span Text=" (*)" TextColor="{DynamicResource DangerousColor}" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <input:SfTextInputLayout
                ErrorText="{Binding RegistrationDate.ErrorFirst}"
                HasError="{Binding RegistrationDate.IsNotValid}"
                VerticalOptions="Start">
                <StackLayout>
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SelectRegisterDateCommand}" />
                    </StackLayout.GestureRecognizers>
                    <Entry
                        InputTransparent="True"
                        Placeholder="DD/MM/YYYY"
                        Text="{Binding RegistrationDate.Value, Converter={StaticResource DateConverter}, Mode=OneWay}"
                        VerticalOptions="Center" />
                </StackLayout>
            </input:SfTextInputLayout>
            <!--  Ngày hết hạn  -->
            <Label HorizontalOptions="StartAndExpand" VerticalOptions="Start">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{i18n:TranslateResource ListVehicle_Label_Vehicle_Expiration_Date}" TextColor="{DynamicResource TextPrimaryColor}" />
                        <Span Text=" (*)" TextColor="{DynamicResource DangerousColor}" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <input:SfTextInputLayout
                ErrorText="{Binding ExpireDate.ErrorFirst}"
                HasError="{Binding ExpireDate.IsNotValid}"
                VerticalOptions="Start">
                <StackLayout>
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SelectExpireDateCommand}" />
                    </StackLayout.GestureRecognizers>
                    <Entry
                        InputTransparent="True"
                        Placeholder="DD/MM/YYYY"
                        Text="{Binding ExpireDate.Value, Converter={StaticResource DateConverter}, Mode=OneWay}"
                        VerticalOptions="Center" />
                </StackLayout>
            </input:SfTextInputLayout>
            <!--  Số ngày cảnh báo trước  -->
            <Label HorizontalOptions="StartAndExpand" VerticalOptions="Start">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{i18n:TranslateResource CabSignInfor_Label_DaysNumberForAlertAppear}" TextColor="{DynamicResource TextPrimaryColor}" />
                        <Span Text=" (*)" TextColor="OrangeRed" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <input:SfTextInputLayout
                ContainerType="Outlined"
                ErrorText="{Binding DaysNumberForAlertAppear.ErrorFirst}"
                HasError="{Binding DaysNumberForAlertAppear.IsNotValid}"
                VerticalOptions="Start">
                <Entry
                    Keyboard="Numeric"
                    MaxLength="3"
                    Placeholder="{i18n:TranslateResource CabSignInfor_Label_DaysNumberForAlertAppear}"
                    Text="{Binding DaysNumberForAlertAppear.Value}" />
            </input:SfTextInputLayout>
            <!--  Ghi chú  -->
            <Label HorizontalOptions="StartAndExpand" VerticalOptions="Start">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{i18n:TranslateResource ReportTemperature_Label_Grid_Description}" TextColor="{DynamicResource TextPrimaryColor}" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <input:SfTextInputLayout
                ErrorText="{Binding Notes.ErrorFirst}"
                HasError="{Binding Notes.IsNotValid}"
                OutlineCornerRadius="20">
                <Editor
                    HeightRequest="100"
                    MaxLength="1000"
                    Placeholder="{i18n:TranslateResource ReportTemperature_Label_Grid_Description}"
                    Text="{Binding Notes.Value}" />
            </input:SfTextInputLayout>
            <Grid Margin="0,10,0,0">
                <StackLayout
                    HorizontalOptions="Center"
                    Orientation="Horizontal"
                    Spacing="20">
                    <Button
                        Command="{Binding SaveCabSignInforCommand}"
                        CornerRadius="20"
                        HeightRequest="40"
                        HorizontalOptions="Center"
                        IsVisible="{Binding SaveButtonVisible}"
                        Text="{i18n:TranslateResource Common_Button_Save}"
                        WidthRequest="150">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="{DynamicResource PrimaryColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="{DynamicResource GrayColor2}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Button>
                    <Button
                        Grid.Column="1"
                        BackgroundColor="{DynamicResource PrimaryColor}"
                        Command="{Binding ChangeToInsertFormCommand}"
                        CornerRadius="20"
                        HeightRequest="40"
                        HorizontalOptions="Center"
                        IsVisible="{Binding CreateButtonVisible}"
                        Text="{i18n:TranslateResource CabSignInfor_Label_Insert}"
                        WidthRequest="150" />
                </StackLayout>
            </Grid>

        </StackLayout>
    </ContentView.Content>
</ContentView>