<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BA_MobileGPS.Core.Views.StreamPicture"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:busyindicator="clr-namespace:Syncfusion.SfBusyIndicator.XForms;assembly=Syncfusion.SfBusyIndicator.XForms"
    xmlns:controls="clr-namespace:BA_MobileGPS.Core.Controls"
    xmlns:ffImage="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    xmlns:prism="http://prismlibrary.com"
    xmlns:vlc="clr-namespace:LibVLCSharp.Forms.Shared;assembly=LibVLCSharp.Forms"
    x:Name="this"
    Title="Camera Stream"
    prism:ViewModelLocator.AutowireViewModel="True">
    <NavigationPage.TitleView>
        <Grid
            ColumnSpacing="10"
            HeightRequest="44"
            HorizontalOptions="FillAndExpand">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label
                Grid.Column="1"
                FontAttributes="Bold"
                FontSize="20"
                HorizontalOptions="Center"
                LineBreakMode="TailTruncation"
                Text="{Binding Source={x:Reference this}, Path=Title}"
                TextColor="{DynamicResource WhiteColor}"
                VerticalOptions="Center" />
        </Grid>
    </NavigationPage.TitleView>
    <Grid
        x:Name="main"
        ColumnDefinitions="*,*"
        ColumnSpacing="10"
        RowDefinitions="*,*"
        RowSpacing="10">

        <ffImage:CachedImage
            Grid.Row="0"
            Grid.Column="0"
            Source="avatar_default.png">
            <ffImage:CachedImage.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ImageTapCommand}" CommandParameter="1" />
            </ffImage:CachedImage.GestureRecognizers>
        </ffImage:CachedImage>

        <ffImage:CachedImage
            Grid.Row="1"
            Grid.Column="0"
            Source="avatar_default.png">
            <ffImage:CachedImage.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ImageTapCommand}" CommandParameter="2" />
            </ffImage:CachedImage.GestureRecognizers>
        </ffImage:CachedImage>

        <ffImage:CachedImage
            Grid.Row="1"
            Grid.Column="1"
            Source="avatar_default.png">
            <ffImage:CachedImage.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ImageTapCommand}" CommandParameter="3" />
            </ffImage:CachedImage.GestureRecognizers>
        </ffImage:CachedImage>

        <ffImage:CachedImage
            Grid.Row="0"
            Grid.Column="1"
            Source="avatar_default.png">
            <ffImage:CachedImage.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ImageTapCommand}" CommandParameter="4" />
            </ffImage:CachedImage.GestureRecognizers>
        </ffImage:CachedImage>
    </Grid>
</ContentPage>